name = "marelune-bali"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = "./dist"

# Environment variables for production (set in Cloudflare dashboard)
[vars]
ENVIRONMENT = "production"
